/**
 * Compatibility re-export. The curated demo dataset now lives in `@/data/demo`.
 * Existing imports of `@/data/demo-routes` keep working; new code should import
 * from `@/data/demo` directly.
 */
export * from "./demo";
